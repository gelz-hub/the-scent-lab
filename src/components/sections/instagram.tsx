// Instagram/social gallery — hidden until an admin-managed Gallery exists
// (Phase 2+). Previously showed hardcoded demo product photos as filler;
// removed rather than replaced with more placeholder imagery, per the
// "no demo content on the storefront" requirement.
export function InstagramSection() {
  return null
}
